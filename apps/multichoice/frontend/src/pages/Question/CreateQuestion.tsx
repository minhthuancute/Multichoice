import React from 'react';
import DefaultLayout from '../../layouts/DefaultLayout';

const CreateQuestion: React.FC = () => {
  return <DefaultLayout>CreateQuestion</DefaultLayout>;
};

export default CreateQuestion;
